services:
  actual_server:
    image: docker.io/actualbudget/actual-server:26.9.0
    container_name: actual_server
    ports:
      - "127.0.0.1:5006:5006"
    environment:
      # https://actualbudget.org/docs/config/oauth-auth/
      ACTUAL_OPENID_DISCOVERY_URL: "https://accounts.google.com/.well-known/openid-configuration"
      ACTUAL_OPENID_AUTH_METHOD: "openid"
      ACTUAL_OPENID_SERVER_HOSTNAME: "https://actualbudget.example.com"
      ACTUAL_OPENID_CLIENT_ID: "<redacted>.apps.googleusercontent.com"
      ACTUAL_OPENID_CLIENT_SECRET: "<redacted>"
      ACTUAL_USER_CREATION_MODE: "manual"
      ACTUAL_OPENID_ENFORCE: "true"
      # Uncomment any of the lines below to set configuration options.
      # - ACTUAL_HTTPS_KEY=/data/selfhost.key
      # - ACTUAL_HTTPS_CERT=/data/selfhost.crt
      # - ACTUAL_PORT=5006
      # - ACTUAL_UPLOAD_FILE_SYNC_SIZE_LIMIT_MB=20
      # - ACTUAL_UPLOAD_SYNC_ENCRYPTED_FILE_SYNC_SIZE_LIMIT_MB=50
      # - ACTUAL_UPLOAD_FILE_SIZE_LIMIT_MB=20
      # See all options and more details at https://actualbudget.org/docs/config/
      # !! If you are not using any of these options, remove the 'environment:' tag entirely.
    volumes:
      # Change './actual-data' below to the path to the folder you want Actual to store its data in on your server.
      # '/data' is the path Actual will look for its files in by default, so leave that as-is.
      - ./actual-data:/data
    healthcheck:
      # Enable health check for the instance
      test: ['CMD-SHELL', 'node scripts/health-check.js']
      # health check using self signed certs
      # test: ['CMD-SHELL', 'NODE_EXTRA_CA_CERTS=/data/selfhost.crt node scripts/health-check.js']
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped

  caddy:
    image: caddy:2.11.4-alpine
    container_name: caddy
    restart: unless-stopped
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./caddy/data:/data
      - ./caddy/config:/config
    ports:
      - '80:80'
      - '443:443'
